import React from "react";
import Layout from "../src/components/layout/layout";
import SectorsPage from "../src/components/sectors-page/sectors-page";

const Sectors: React.FC = () => {
    return (
        <Layout>
            <SectorsPage />
        </Layout>
    );
};

export default Sectors;
